# Model GGUF files - too large for git
models/

# llama.cpp binary directory - too large for git
llama.cpp-b9360-cuda12/
llama.cpp-b10437-cuda12/

# Downloaded zip files
*.zip

# Runtime logs (not useful in repo)
*.log
*.pid

# Runtime error logs
*.err.log

# Benchmark JSON results (machine-specific, regenerate locally)
benchmark_results/*.json

# Benchmark summaries are tracked (not ignored)

# Reddit API credentials — NEVER commit
reddit.txt
.reddit_token.json
.gitignore_for_token

# OMC session state (not project content)
.omc/
